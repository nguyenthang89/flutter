import 'package:flutter/cupertino.dart';

class SubMenu extends StatelessWidget {
  //final String name;
  //const Submenu(this.name);

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
